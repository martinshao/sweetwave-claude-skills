# N9: Context Reload

在 `RUN_STATE.md` 写入最小上下文恢复摘要：

```md
## 上下文恢复摘要

- 当前模块：
- 刚完成任务：
- 下一任务：
- 必读规格：
- 相关 LESSONS：
- Git 基线：
- 未解决风险：
- 推荐恢复命令：
```

后续任务只加载摘要指定的模块规格、任务正文、相关 LESSONS 和必要代码。
环境支持时可在状态落盘后清理会话上下文；不支持时继续执行，但不得依赖旧聊天内容。

将当前节点写为 `N9_CONTEXT_RELOAD`。
