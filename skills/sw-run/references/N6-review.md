# N6: Review

1. 确认任务为 `[REVIEWING]`，且当前验证结论为 `PASSED`。
2. 将当前节点写为 `N6_REVIEW`。
3. 只审查当前任务相关 diff，检查正确性、架构边界、测试缺口、安全和无关改动。
   scaffold 任务额外检查规划路由覆盖、占位页无业务实现、现有真实功能未被删除、
   示例代码删除证据和未新增依赖。
4. Must Fix 返回 N4 修复，并使旧验证结论失效。
5. 安全疑点写入 N7 的强制安全原因。
6. 审查通过后进入 N7，不得直接写 `[x]`。

`--stage review` 完成审查后：

- 无额外 QA/安全门：执行 N8 后停止。
- 有额外门禁：停在 N7，恢复命令为 `/sw-run {module} {TASK-ID} --stage qa`。
